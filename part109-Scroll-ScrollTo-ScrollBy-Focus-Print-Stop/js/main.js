/*
  BOM [Browser Object Model]
  - stop() // stops the current document from loading
  - print() // prints the current document
  - focus() // focuses on the current window
  - scrollTo(x, y || Options) // scrolls to a particular set of coordinates in the document supported in all browsers
  - scroll(x, y || Options) // is the same as scrollTo() method but it does not support in all browsers (IE, Safari)
  - scrollBy(x, y || Options) // scrolls the document by the specified number of pixels relative to the current scroll position supported in all browsers
*/

// let myNewWindow = window.open("https://google.com", "", "width=500,height=500");

// window.scrollTo({
//   left: 500,
//   top: 200,
//   behavior: "smooth"
// });